{
 "format": "asqav-native",
 "outcome": "verified",
 "reason_code": "",
 "notes": "asqav-01's valid genesis receipt with one anchor entry carrying the optional informational `status` member (draft anchors[] schema): status pending declares a not-yet-anchored proof, so the verifier must not count it as a trusted anchor. The signature is untouched (anchors sit outside the signed bytes), so the oracle verdict stays verified; under the full verifier the anchors axis reports SKIPPED (unverifiable), never a PASS on presence."
}
